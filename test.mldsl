event(вход) {
    hello ~ 1 / 2 * 2
    if_player.проверить_режим_игры(режим_игры="Creative") {
        # player.message("hi")
    }
    
    if 2 > 5 {

    }
}
function hello {
    # player.message("hiв")
}