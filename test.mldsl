event(вход) {
    hello ~ 1 / 2 * 2
    if_player.режим_игрока(режим_игры="Creative") {
        # player.message("hi")
    }
    
    if 2 > 5 {

    }
    
}   
function hello {
    # player.message("hiв")
}